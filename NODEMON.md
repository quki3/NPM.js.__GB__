# nodemon
## istalacion
```bash
npm install --save-dev nodemon
npm i nodemon -D                //? "-D || --save-dev le dice que istale nodemon como una dependencia de desarrollo no lo istala
                                para produccion si te fijas en package.json veras que no esta como dependencia"
```
